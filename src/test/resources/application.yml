spring:
  config:
    name: KoNoMaSTest
  aop:
    proxy-target-class: false
  main:
    banner-mode: off

server:
  port: 8181

management:
  endpoint:
    info:
      enabled: false
    health:
      enabled: false
      probes:
        enabled: false
  health:
    cassandra:
      enabled: false
